/****   SLVG_Global.h**   Header containing globaly used defines, macros, and *   type declerations***   Copyright © 1993 Bill Hensler, *   All rights reserved worldwide.**   Original:   Bill Hensler*   Created:    11/20/93 10:22 PM*   ***/#pragma once#ifndef NULL    // make sure null is defined as other than 0L    #define     NULL        ((void *) 0)    #endif// Maximum value a signed short can be#define kMaxShort   32767   // Mininum value a signed short can be#define kMinShort   -32768 // Maximum value a signed long can be#define kMaxLong    0x7FFFFFFF  // Minimum value a signed long can be   #define kMinLong    0x80000000      // Macro for absolute value of any number#define Abs(x)  ((x) < 0 ? -(x) : (x))// Macro for the minimum and maximum of any two numbers#define Max(x, y)    ((x) > (y) ? (x) : (y))#define Min(x, y)    ((x) < (y) ? (x) : (y))typedef struct{    Boolean     hasWNE          : 1;    Boolean     hasColorQD      : 1;    Boolean     hasGestalt      : 1;    Boolean     hasAppleEvents  : 1;    Boolean     hasAliasMgr     : 1;    Boolean     hasEditionMgr   : 1;    Boolean     hasHelpMgr      : 1;    Boolean     hasScriptMgr    : 1;    Boolean     hasFPU          : 1;    Boolean     has32BitQD      : 1;    Boolean     hasQuickTime    : 1;    short       scriptsInstalled;    short       systemVersion;    } SystemStatsRec;extern SystemStatsRec gSystemStats;